# Displaying Data: Tables, Charts, Metrics — Solution Download

**GitHub:** `module-06-web-streamlit/solutions/exercises/data-explorer/`

Compare your solution to the reference. Key things to check:

- Is `@st.cache_data` on your fetch function?
- Do the metrics update when you filter by name in the sidebar?
- Is the dataframe interactive (`st.dataframe()`, not `st.table()`)?
- Does the bar chart show users grouped by city?

Differences in layout, additional features, and column choices are fine. The core requirement is: cached fetch, 3 metrics, filtered dataframe, and a chart.