# CSS Essentials — Solution Download

**GitHub:** `module-06-web-streamlit/solutions/exercises/styled-portfolio/`

Compare your solution to the reference. Key things to check:

- Did you use `box-sizing: border-box` in your reset?
- Is your Flexbox container using `display: flex`, `gap`, and `flex-wrap: wrap`?
- Do you have `:hover` states on interactive elements (links, cards)?

Your color choices, font sizes, and specific spacing values will differ from the reference — that's expected and encouraged. There's no single "correct" design. The important thing is that the layout structure (centered content, card grid, matching header/footer) is solid.