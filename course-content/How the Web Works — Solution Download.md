# How the Web Works — Solution Download

**GitHub:** `module-06-web-streamlit/solutions/exercises/api-explorer/`

Compare your solution to the reference. Key things to check:

- Did you handle the 404 case by checking `response.status_code` *before* calling `response.json()`?
- Did you extract the type names from the nested `types` list structure?
- Did you use a loop to avoid repeating code for each Pokémon?

Differences in formatting or variable names are perfectly fine — there are many valid approaches as long as the output is correct and errors are handled gracefully.